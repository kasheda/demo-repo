name: CI Test
on: [push]  # Triggers on push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - run: |
        echo "Starting test"
        exit 1  # This will cause the job to fail
        echo "This should never run"