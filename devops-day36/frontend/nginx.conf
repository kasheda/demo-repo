server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    location /api/ {
        proxy_pass http://${DAY36_BACKEND_HOST}:${DAY36_BACKEND_PORT}/api/;
    }
}
